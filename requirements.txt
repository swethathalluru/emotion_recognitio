# ── Face Expression Detection — Python dependencies ───────────────────────────
#
# Install with:
#   pip install -r requirements.txt
#
# Tested on Python 3.10 / 3.11 (3.12 works but mediapipe wheels may lag).
# Use a virtual environment:
#   python -m venv .venv && source .venv/bin/activate   (Linux/macOS)
#   python -m venv .venv && .venv\Scripts\activate      (Windows)

# ── Core vision & ML ──────────────────────────────────────────────────────────
mediapipe==0.10.14          # FaceMesh, Hands, Pose solutions
opencv-python==4.10.0.84    # Webcam capture + drawing
numpy>=1.26,<2.0            # Array ops (mediapipe pins <2.0)

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn>=1.4,<2.0      # SVM, RandomForest, GradientBoosting, pipelines
joblib>=1.3                 # Model serialisation (already a sklearn dep)

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.1                 # Reading FER2013 CSV, landmark CSVs
tqdm>=4.66                  # Progress bars for batch extraction

# ── Optional: deep-learning classifier (uncomment if you want CNN/MLP) ────────
# torch>=2.2                # PyTorch — GPU-accelerated MLP classifier
# torchvision>=0.17         # Image transforms for raw-pixel CNN baseline

# ── Optional: experiment tracking ─────────────────────────────────────────────
# mlflow>=2.12              # Run tracking, model registry
# tensorboard>=2.16         # Loss / metric visualisation

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0